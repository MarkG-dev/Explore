# The 307 MB uncompressed archive is not committed — it exceeds GitHub's 100 MB
# per-file limit. Nothing is lost: the complete file lives in the repo as four
# gzipped shards under decks_hearthpwn_shards/ (33.7 MB total), which reassemble
# to this exact file, verified by SHA-256.
#
#   python3 scripts/shard_decks.py --verify                    # prove losslessness
#   python3 scripts/shard_decks.py --restore <this-filename>   # rebuild it
#   zcat decks_hearthpwn_shards/decks_hearthpwn.part*.ndjson.gz > <this-filename>
decks_NJacobsohn-Hearthstone-Data-Analysis.ndjson

# local scratch
_verify.tmp
