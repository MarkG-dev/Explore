Document header bar — two hairline-topped columns with mono labels and an index marker. Used on brand collateral and slides.

```jsx
<PageHeader left="LOOP X" section="BRAND GUIDELINES" index="02" />
<PageHeader tone="light" index="05" />
```
