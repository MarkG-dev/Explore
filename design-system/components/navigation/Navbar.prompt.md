Site navigation — the LOOP X wordmark with a row of chamfered mono nav items; the active item is a solid pill.

```jsx
<Navbar active="PRODUCT" onSelect={setPage} />
<Navbar tone="dark" items={["HOME","PLATFORM","COMPANY"]} />
```

Use `tone="light"` over dark heroes (default), `tone="dark"` on light pages.
